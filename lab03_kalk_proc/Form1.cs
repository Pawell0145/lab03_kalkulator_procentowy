namespace lab03_kalk_proc
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
            this.Text = "Kalkulator procentowy";
        }
        /*
        double vol = double.Parse(pojemnosc_substancji_tutaj.Text);
        double percent = double.Parse(procent_substancji_tutaj.Text);
        int count = int.Parse(ilosc_naczym_tutaj.Text);

        user_input input = new user_input(vol, percent, count);

        tu_wstawisz_nazwe_pola_w_ktorym_bedzie_wyswietlany_wynik_obj_plynu.Text = input.Subst_sum().ToString();
        tak_samo_jak_powyrzej_tylko_dla_samej_czystej_substancji.Text = input.Clean_subst_vol().ToString();
        */
        
    }
}
